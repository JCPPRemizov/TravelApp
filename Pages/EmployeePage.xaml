<Page x:Class="TravelApp.Pages.EmployeePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:TravelApp.Pages"
      mc:Ignorable="d" 
      Title="EmployeePage" Loaded="Page_Loaded">

    <Grid>
        <StackPanel Orientation="Horizontal">
            <DataGrid x:Name="EmployeeDataGrid" Style="{StaticResource MaterialDesignDataGrid}" BorderBrush="Black" Margin="0 0 10 0">

            </DataGrid>
            <StackPanel Orientation="Vertical">
                <TextBox x:Name="SurNameEmployee" materialDesign:HintAssist.Hint="Введите фамилию сотрудника" materialDesign:HintAssist.HelperText="Фамилия сотрудника" Style="{StaticResource MaterialDesignOutlinedTextBox}" FontSize="14" Width="300" Margin ="0 0 0 20"/>
                <TextBox x:Name="NameEmployee" materialDesign:HintAssist.Hint="Введите имя сотрудника" materialDesign:HintAssist.HelperText="Имя сотрудника" Style="{StaticResource MaterialDesignOutlinedTextBox}" FontSize="14" Width="300"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
